import{useQuery}from"@tanstack/react-query";import{Link}from"react-router-dom";import{StatusBadge}from"../components/StatusBadge";import{applicationService}from"../services/applicationService";export function AdminDashboard(){const{data=[]}=useQuery({queryKey:["applications"],queryFn:applicationService.listApplications});const count=(s:string)=>data.filter(x=>x.status===s).length;const stats:[string,number][]=[["Total Applications",data.length],["Submitted",count("SUBMITTED")],["Under Review",count("UNDER_REVIEW")],["Change Requested",count("CHANGE_REQUESTED")],["Approved",count("APPROVED")],["Rejected",count("REJECTED")]];return <section><div className="page-heading"><div><p className="eyebrow">Admissions</p><h1>Dashboard</h1></div></div><div className="stats">{stats.map(([l,v])=><article className="card" key={l}><span>{l}</span><strong>{v}</strong></article>)}</div><article className="panel"><div className="panel-heading"><h2>Recent applications</h2><Link to="/admin/applications">View all</Link></div><div className="table-wrap"><table><thead><tr><th>Application</th><th>Applicant</th><th>Program</th><th>Status</th><th></th></tr></thead><tbody>{data.map(x=><tr key={x.id}><td>{x.applicationNo}</td><td>{x.applicantName}</td><td>{x.program}</td><td><StatusBadge status={x.status}/></td><td><Link to={`/admin/applications/${x.id}`}>Open</Link></td></tr>)}</tbody></table></div></article></section>}
