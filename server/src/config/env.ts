import{z}from"zod";const schema=z.object({PORT:z.coerce.number().int().positive().default(4000),DATABASE_URL:z.string().min(1),CLIENT_ORIGIN:z.string().url().default("http://localhost:5173"),JWT_SECRET:z.string().min(32),COOKIE_SECURE:z.string().default("false").transform(v=>v==="true")});export const env=schema.parse(process.env);