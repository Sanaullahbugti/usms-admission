services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: usms
      POSTGRES_PASSWORD: usms
      POSTGRES_DB: usms_admission
    ports:
      - "5432:5432"
    volumes:
      - usms_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U usms -d usms_admission"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  usms_postgres_data:
